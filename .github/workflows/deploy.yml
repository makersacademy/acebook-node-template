name: Deploy

on:
    workflow_run:
        workflows: ['Build and Test']
        branches: [main]
        types: [completed]

jobs:
    deploy:
        runs-on: ubuntu-latest
        if: ${{ github.event.workflow_run.conclusion == 'success' }}
        steps:
        - run: echo 'Hello Bereket and Lil'
            
    