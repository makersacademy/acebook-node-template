<!DOCTYPE html>
<html>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <head>
    <link href="../public/stylesheets/style.css" rel="stylesheet" type="text/css">
  </head>
    <body> 
      <nav class="nav">
        {{#if user}}
          <h6>You are signed in as: {{user.firstName}}</h6>
        {{else}}
        {{/if}}
          <img id="profile_image" src="{{user.image.imgPath}}" style="width:5%" alt="profile pic">
       
        <a class="a" href="/">Home</a>
        <a class="a" href="/posts">Posts</a>
        <a class="a" href="/posts/new">New posts</a>
        <a class="a" href="/">Log Out</a>
        <a class="a" href="/photos">Snaps</a>
      </nav>
      
<h1>Timeline</h1>
<ul class="posts">
  {{#each posts}}
  <li>{{this.message}}</li>
    <li>{{this.user}}</li>
    <img id="post_image" src="{{this.userImage}}" style="width:2%" alt="">
    <li>{{dateFormat this.createdAt}}</li>
    <li>likes: {{this.likes}}</li>
  <ul>
    {{#each this.comments}}
      <li>{{this.note}}</li>
      <li>{{this.user}}</li>
    {{/each}}
  </ul>
    <form action='/posts/comment/{{this.id}}' method='GET'>
      <input type="submit" value="Comment">
    </form>
     <form action='/posts/delete/{{this.id}}' method='POST'>
      <input type="submit" value="Delete">
    </form>
    <form action='/posts/like/{{this.id}}' method='POST'>
      <input type="submit" value="Like">
    </form>
  {{/each}}
</ul>
<a class="new-post-link" href="/posts/new">New post</a>
  </body>
 </html>