<h1>Timeline for {{ name }} </h1>
 <h4 class="name">{{user.name}}</h4>

<div class="posts" >
  {{#each posts}}
  <div>{{this.user.name}} <br>
  {{#if user.image}}
  <img src=/uploads/profileimage/{{user.image}} style="height:60px; width:60px;">
  {{else}}
  <img src=/uploads/default/{{user.defaultImage}} style="height:60px; width:60px;">
  {{/if}}

   {{this.message}}  <br>
   {{#if this.imagePath}}
  <img height="200" src="{{this.imagePath}}"> <br> 
   {{/if}}
  <div id="createdAt">{{this.postedAt}}</div> <br> 
  <form action="/posts/like/{{this._id}}" method="post" id = "like_{{this._id}}">
    <button id="like" name="postId" value="{{this._id}}">Like</button>
  </form> 
  <div id="likecount">{{this.likes}}</div> 
  </div>
  <br>
  {{> comments}}
  {{/each}}
<div>





